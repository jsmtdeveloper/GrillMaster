/**
 * Typed params than we use on the methods than checks if a items fits.
 *
 * @example
 * {itemWidth: number;itemLength: number; grillSpace: string[][];}
 */
export type FitsOnGrillParams = {
  itemWidth: number;
  itemLength: number;
  grillSpace: string[][];
};
